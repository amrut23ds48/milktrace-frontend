// hooks/index.ts
// ─── Custom React Hooks ───────────────────────────────────────────────────────
// All shared custom hooks live here.
// Hooks encapsulate stateful logic for reuse across multiple components.
// Prefer SWR or React Query hooks for server state (see FRONTEND_GUIDELINES.md §4).
//
// Example: export { useAuth } from './useAuth';
